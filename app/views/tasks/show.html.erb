  <p><%= @task.id %></p>
  <p>タスク名: <%= @task.name %></p>
  <p>内容: <%= @task.content %></p>
  <p>ステータス: <%= @task.status %></p>
  <p>〆切: <%= @task.deadline.strftime("%m月%d日") %></p>

  <%= link_to "編集する", edit_task_path(@task), class: 'btn btn-warning' %>
  <%= link_to "削除する", @task, method: :delete, class: 'btn btn-warning' %>